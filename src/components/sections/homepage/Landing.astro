---
import { getImage } from 'astro:assets'
import myBackground from '@/assets/mlodzi.avif'
import { Icon } from 'astro-icon/components'

const optimizedBackground = await getImage({
  src: myBackground,
  quality: 80,
  loading: 'eager',
  format: 'avif',
})
---

<section
  class='flex relative w-full bg-cover min-h-screen items-center justify-center'
  style={`background-image: url(${optimizedBackground.src}); background-position: center 40%;`}
>
  <div class='flex flex-col items-center p-4 backdrop-blur-sm xl:mt-16 gap-4'>
    <h1 class='text-sm font-semibold'>Fotograf okolicznościowy Puchaczów</h1>
    <p
      class='sour-gummy-semibold text-4xl xl:text-[96px] tracking-wide text-center text-primary'
    >
      Emocje i uczucia w kadrze
    </p>
    <p class='text-lg lg:text-xl font-medium max-w-5xl text-center'>
      Sekretem fotografii jest uchwycenie chwili w idealny sposób, co pozwala na
      długie i radosne wspominanie utrwalonych momentów. To szczególnie ważne w
      fotografii okolicznościowej, gdzie zapisane obrazy stają się bezcenną
      pamiątką ważnych wydarzeń, takich jak śluby, chrzciny czy jubileusze.
      Dzięki nim możemy raz za razem wracać do tych wyjątkowych emocji i
      wspomnień.
    </p>
    <div
      class='flex mt-6 flex-col md:flex-row gap-4 text-xl xl:text-2xl w-full justify-center items-center sour-gummy-semibold'
    >
      <a
        href='/kontakt'
        class='flex bg-primary rounded-full items-center hover:scale-105 hover:shadow-xl duration-300 transition-all text-accent text-base w-max xl:text-2xl py-4 px-8'
      >
        Zarezerwuj termin
      </a>
      <a
        href='/galeria'
        class='flex bg-primary rounded-full items-center hover:scale-105 hover:shadow-xl duration-300 transition-all text-accent text-base w-max xl:text-2xl py-4 px-8'
      >
        Obejrzyj zdjęcia
      </a>
    </div>
  </div>
</section>

<style>
  .double-brand-clip {
    clip-path: polygon(25% 0%, 100% 0%, 75% 100%, 0% 100%);
  }
</style>
